# Pip dependencies for sobits_vla_tools.
#
# Single source of truth consumed by install.sh (which templates the
# lerobot line so LEROBOT_VERSION_SPEC keeps working as an env override —
# see install.sh for how this file is parsed).
#
# torch is intentionally absent: install.sh only installs it if missing,
# and leaves version selection to TORCH_VERSION_SPEC / the environment
# (e.g. a pre-installed CUDA-matched wheel).

huggingface_hub
lerobot[training,transformers-dep,pi,smolvla,peft-dep]~=0.6.0
peft
numpy>=2.0.0,<2.3.0
# pandas must be a pip build compatible with numpy 2.x — Ubuntu's apt
# python3-pandas (2.1.x, built against numpy 1.x) otherwise "satisfies" the
# dependency and crashes at import with "numpy.dtype size changed" ABI
# errors. >=2.2.2 is the first numpy2-compatible pandas and forces a pip
# install even when the system package exists. numexpr/bottleneck pins
# below exist for the same numpy-2 compatibility reason.
pandas>=2.2.2,<2.4
numexpr>=2.10.2
bottleneck>=1.4.2
pyyaml
rosbags
scipy
